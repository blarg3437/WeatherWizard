package com.example.weatherwizard.ViewModel

import androidx.lifecycle.ViewModel

/**
 * this will contain all state-sensitive data within the application,
 * including but not limited to: list of WeatherScollItems, selected page preferences,
 * current map view data, ...
 */
class WeatherViewModel: ViewModel() {
}